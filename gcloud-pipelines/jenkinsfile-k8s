pipeline {
    agent any

    stages {
        stage('Clone Gcloud Repo') {
            steps {
                sh 'git clone https://github.com/GoogleCloudPlatform/microservices-demo.git'
            }
        }

        stage('Check Dependencies') {
            steps {
                sh 'helm version'
                sh 'kubectl version'
                sh 'skaffold version'
            }
        }

        // stage('Check Dependencies') {
        //     steps {
        //         sh 'gcloud services enable container.googleapis.com'
        //         sh 'gcloud container clusters get-credentials devo-gke-cluster --region=us-west1'
        //     }
        // }

        // stage('Check Dependencies') {
        //     steps {
        //         dir ("microservices-demo"){
        //             sh 'terraform init -upgrade'
        //         }
        //     }
        // }
    }
}
