pipeline {
    agent any

    stages {
        stage('Kubernetes deployment') {
            steps {
                dir ("gcloud-microservices"){
                    sh 'terraform init -upgrade'
                }
            }
        }
    }
}
