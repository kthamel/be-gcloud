pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'git clone https://github.com/GoogleCloudPlatform/microservices-demo.git'
            }
        }
    }
}
